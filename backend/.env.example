# Django Settings
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Email (production)
# EMAIL_BACKEND=django.core.mail.backends.smtp.SmtpEmailBackend
# EMAIL_HOST=smtp.health.qld.gov.au
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
